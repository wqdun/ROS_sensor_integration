//
// Created by root on 18-5-19.
//

#include "Viewer.h"
#include <pangolin/pangolin.h>

/*Viewer2::Viewer2(vinssystem* pSystem):mpSystem(pSystem)
{
    float fps = 30;
    mT = 1e3/fps;
}

void Viewer2::Run()
{
    cv::namedWindow("VINS-PC2: Current Frame", CV_WINDOW_NORMAL);
    while(true)
    {
        if(!mpSystem->mimText.empty())
        {
            cv::imshow("VINS-PC2: Current Frame",mpSystem->mimText);
            cv::waitKey(mT);
        }
    }
}*/